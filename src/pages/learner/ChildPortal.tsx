import { Navigate } from 'react-router-dom';

// Placeholder — child portal will be rebuilt for African History curriculum
export default function ChildPortal() {
  return <Navigate to="/dashboard" replace />;
}
