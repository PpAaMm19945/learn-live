import { Navigate } from 'react-router-dom';

// Placeholder — learner dashboard will be rebuilt for African History curriculum
export default function LearnerDashboard() {
  return <Navigate to="/dashboard" replace />;
}
