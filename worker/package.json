{
    "name": "learnlive-worker",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npx wrangler dev",
        "deploy": "wrangler deploy",
        "build": "wrangler deploy --dry-run"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240320.1",
        "typescript": "^5.4.3",
        "wrangler": "^3.42.0"
    },
    "dependencies": {
        "@aws-sdk/client-s3": "^3.1008.0"
    }
}
