# P3D_quiz_progress

- Implemented `QuizCard` and `QuizSession` for interactive quiz flow and scoring.
- Created `LessonProgress` badge component for quick status visualization.
- Built `ProgressOverview` utilizing recharts to display topic scores and general metrics.
- Adhered strictly to semantic design tokens.
- Components submit and fetch from API directly with included credentials.
